import { Button } from "@mui/material";
import { NextPage } from "next";

const Home: NextPage = () => {
  return (
      <Button variant="contained" color="warning">
        Hello World
      </Button>
  );
};

export default Home;
